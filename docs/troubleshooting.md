# Troubleshooting

## install_dependencies() Function

If the `install_dependencies()` function fails to install missing dependencies, it is likely due to insufficient permissions. To resolve this issue, ensure that the editor is run in administrator mode. This will allow the function to install the required dependencies successfully.